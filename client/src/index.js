import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  // TODO go back to strict mode after later
  // <React.StrictMode>
  //   <App />
  // </React.StrictMode>
  <App />
);
